import { BrowserRouter, Routes, Route } from "react-router-dom";

export const Routers = () => {
  return (
    <BrowserRouter>
      <Routes>
        <Route />
      </Routes>
    </BrowserRouter>
  );
};
